# 架构设计文档

## 数据处理

### 文档分块策略

| 策略 | 做法 | 适用 |
|------|------|------|
| **递归语义（默认）** | 先合并段落直到接近 2000 字，超长段按句子切，单句超长才按字符 | 通用文档 |
| **Markdown 标题** | 按 `##` 标题分段，每段独立完整 | 结构化 Markdown |
| **固定字符** | 每 N 字符一切，固定 overlap | 无结构纯文本 |

### 嵌入模型

Qwen3-Embedding-4B，2560 维，本地 CPU 运行。启动时后台预加载，不阻塞请求。连续调用加锁防止崩溃。

### 检索流程

```
简单事实问题（"CEO是谁"）:
  向量搜索 → 回答 → 跳过 Reflection → ~5s，1 次 LLM 调用

复杂推理问题（"为什么 Transformer 更好"）:
  知识库检索 → ReAct 循环 → Reflection 审核 → ~20s，4-5 次 LLM 调用
```

## 记忆系统

| 类型 | 存储 | 生命周期 | 触发 |
|------|------|----------|------|
| 会话历史 | SQLite conversations/messages 表 | 永久 | 所有对话自动记录 |
| 长期记忆 | SQLite memories 表 | 永久 | 用户说"上次/刚才/之前"时触发搜索 |
| 知识库索引 | SQLite doc_chunks 表 | 永久 | 上传文档时嵌入 |

## 性能

| 阶段 | 耗时 |
|------|------|
| Qwen3 模型加载 | ~20s（启动时后台进行） |
| 简单事实查询 | ~4-5s（1 次 LLM + 1 次嵌入搜索） |
| 向量搜索 | ~600ms（单次嵌入） |

## 日志系统

三层日志分离，每日轮转，保留 30 天：

- `qa_backend.log` — 全量日志
- `qa_backend_error.log` — ERROR + WARNING
- `upload.log` — 文档上传操作专用

每条请求记录完整的 [TIMING] 链路耗时。
